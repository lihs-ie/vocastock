query LearningStateQuery($identifier: ID!) {
  learningState(identifier: $identifier) {
    vocabularyExpression
    proficiency
  }
}

query LearningStatesQuery {
  learningStates {
    vocabularyExpression
    proficiency
  }
}
