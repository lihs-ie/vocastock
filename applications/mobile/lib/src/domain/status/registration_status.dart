/// Registration status on a VocabularyExpression (CLAUDE.md / constitution §VI).
///
/// Distinct from generation status, proficiency, frequency, and sophistication.
enum RegistrationStatus {
  active,
  archived,
}
