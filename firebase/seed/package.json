{
  "name": "vocastock-firebase-seed",
  "version": "0.1.0",
  "private": true,
  "description": "Seed fixture data for the vocastock Firebase emulator suite.",
  "type": "module",
  "scripts": {
    "seed": "node seed.mjs",
    "seed:reset": "node seed.mjs --reset"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  }
}
