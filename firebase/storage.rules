rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Local emulator baseline: repository bootstrap keeps rules permissive.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
