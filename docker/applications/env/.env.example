# API listener ports
# Default values avoid the Firebase Firestore emulator host port (18080).
GRAPHQL_GATEWAY_PORT=18180
COMMAND_API_PORT=18181
QUERY_API_PORT=18182

# Shared API contract
VOCAS_READINESS_PATH=/readyz
RUST_LOG=info

# Worker stable-run contract
VOCAS_WORKER_STABLE_RUN_SECONDS=10
VOCAS_WORKER_POLL_INTERVAL_SECONDS=30

# Optional local dependency references
VOCAS_FIREBASE_PROJECT=demo-vocastock
PUBSUB_EMULATOR_HOST=
FIRESTORE_EMULATOR_HOST=
STORAGE_EMULATOR_HOST=
FIREBASE_AUTH_EMULATOR_HOST=

# Flip to `true` to force command-api / query-api / workers to use the
# Firestore / PubSub / Firebase Auth adapters instead of the in-memory
# fixtures. Leave blank (or `false`) to keep the existing feature tests
# and local dev flow bound to the deterministic stubs.
VOCAS_PRODUCTION_ADAPTERS=

# Worker mock switch — when set, explanation / image workers stay on
# deterministic outcomes instead of calling Anthropic / Stable Diffusion.
USE_MOCK_LLM=true
LLM_PROVIDER=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
STABILITY_API_KEY=
STRIPE_SECRET_KEY=

# Optional internal routing defaults
VOCAS_COMMAND_UPSTREAM_BASE_URL=http://command-api:18181
VOCAS_QUERY_UPSTREAM_BASE_URL=http://query-api:18182
